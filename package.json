{
  "name": "vibehard",
  "version": "0.0.0",
  "private": true,
  "description": "Safe vibe coding — an enforced gate chain between an AI app generator and deploy.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "bun": ">=1.1.0"
  },
  "workspaces": ["packages/*"],
  "bin": {
    "vibehard": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "start": "bun web/server.ts"
  },
  "devDependencies": {
    "@ai-sdk/provider": "^3.0.10",
    "@types/bun": "^1.1.14",
    "fast-check": "^4.8.0",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.85",
    "@ai-sdk/openai-compatible": "^2.0.51",
    "@aws-sdk/client-s3": "^3.1085.0",
    "@clerk/backend": "^3.8.4",
    "@electric-sql/pglite": "^0.5.3",
    "@vibehard/gate-check": "workspace:*",
    "@vibehard/orchestrator": "workspace:*",
    "ai": "^6.0.208",
    "postgres": "^3.4.9"
  }
}
