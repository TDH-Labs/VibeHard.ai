/**
 * Re-export shim (2026-07-10 extraction) — now lives in @vibehard/gate-check.
 * Kept at this path so every existing internal import (`../gate/build-errors.ts`) needs zero changes.
 */
export { parseBuildErrors } from "@vibehard/gate-check";
