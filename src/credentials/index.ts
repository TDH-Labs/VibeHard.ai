/** External-credential detection (backlog #5) — barrel export. */
export { collectAppEnv, parseEnvExampleKeys, requiredCredentials, requiredCredentialsForApp, type RequiredCredential } from "./credentials.ts";
