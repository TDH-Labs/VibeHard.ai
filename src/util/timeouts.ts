/**
 * Re-export shim (2026-07-10 extraction) — now lives in @vibehard/gate-check.
 * Kept at this path so every existing internal import (`../util/timeouts.ts`) needs zero changes.
 */
export { SUBPROCESS_TIMEOUT_MS } from "@vibehard/gate-check";
